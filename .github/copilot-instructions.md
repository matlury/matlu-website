# Copilot Instructions

- Never read, search, open, or modify files ignored by `.gitignore`.
- Never use tooling options that include ignored files (for example, `includeIgnoredFiles=true`).
- If a task would require access to gitignored files, stop and ask the user for an alternative source.
